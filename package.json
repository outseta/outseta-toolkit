{
  "name": "outseta-toolkit",
  "private": true,
  "description": "Outseta SDK monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
